import { Then } from "@cucumber/cucumber";

Then("the likert-scale should have five options", async function() {
    return "pending";
});

Then("the likert-scale's start label should be {string}", async function(string_0) {
    return "pending";
});

Then("the likert-scale's end label should be {string}", async function(string_0) {
    return "pending";
});
